// HOME COMPONENT (STATELESS)
const Home = () => {
    return (
        // Render the main part of the / route
        <main>
            <h2>HOME Page</h2>
        </main>
    );
};

// EXPORT HOME COMPONENT
export default Home;
